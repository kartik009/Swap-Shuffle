<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SwapShuffle.View.HistoryProductPage">
    <ContentPage.Content>
        <StackLayout Padding="5">
            <Image Source="{Binding p_image}" />
            <!--<Label>
                <Label.FormattedText/>
            </Label>-->
            <Label Text="{Binding Name}" />
            <!--<Label Text="{Binding Title}" />-->
            <Label Text="{Binding cname}"/>
            <Label Text="{Binding price}" />
            <Label Text="{Binding p_description}" />
            <BoxView HeightRequest="3" 
            BackgroundColor="DarkSeaGreen" 
            HorizontalOptions="FillAndExpand" ></BoxView>
            <Label x:Name="lb_userid" />
            <Label x:Name="lb_uname" />
            <Label x:Name="lb_date" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
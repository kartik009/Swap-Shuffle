<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SwapShuffle.View.ViewProductDetailPage">
    <ContentPage.Content>
        <StackLayout>
            <Image Source="{Binding p_image}" />
            <!--<Label>
                <Label.FormattedText/>
            </Label>-->
            <Label Text="{Binding Name}" />
            <Label Text="{Binding Title}" />
            <Label Text="{Binding price}" />
            <Label Text="{Binding cname}" />
            <Label Text="{Binding p_description}" />

            <Button x:Name="Btn_Buy" Text="Buy" Clicked="Btn_Buy_Clicked" Margin="0,10,0,0" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>